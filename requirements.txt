# ── 数值计算 ────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
pandas>=1.5.0

# ── 数据存储 / 配置 ─────────────────────────────────────────────────
h5py>=3.8.0
pyyaml>=6.0
tqdm>=4.65.0

# ── 物理模拟器 ──────────────────────────────────────────────────────
flopy>=3.7.0              # MODFLOW 地下水（抛物型PDE）
pandapower>=2.13.0        # 稳态潮流（非线性代数）
andes>=2.0.0              # 暂态稳定（DAE）
pypsa>=0.26.0             # 能源-气候优化（动态代数LP）
linopy>=0.3.0             # PyPSA 的 LP 建模层（pypsa.optimize() 依赖）
highspy>=1.5.0            # HiGHS LP 求解器（PyPSA 后端）
# SimPEG 使用纯 numpy 1D 解析实现，无需安装 simpeg 包

# ── Web 后端 ────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# ── HTTP 客户端（LLM API 调用）──────────────────────────────────────
requests>=2.28.0

# ── 开发 / 测试 ─────────────────────────────────────────────────────
pytest>=7.0.0
ruff>=0.3.0
