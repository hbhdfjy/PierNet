# 数值计算基础依赖，供仿真、样本生成和训练共享
numpy>=1.24.0
scipy>=1.10.0
pandas>=1.5.0

# 数据读写 / 配置解析 / 进度展示
h5py>=3.8.0
pyyaml>=6.0
pyarrow>=15.0.0
duckdb>=1.0.0
tqdm>=4.65.0

# 五类模拟器依赖
flopy>=3.7.0              # MODFLOW 地下水抛物型 PDE
pandapower>=2.13.0        # 电力潮流仿真
andes>=2.0.0              # 电力暂态 DAE 仿真
pypsa>=1.1.0,<1.2.0       # 能源-气候优化 LP 求解
linopy>=0.6.0,<0.7.0      # PyPSA 的 LP 后端，需与 pypsa.optimize() 兼容
highspy>=1.5.0            # HiGHS LP 求解器，供 PyPSA 调用
xarray>=2025.1.0,<2026.0.0  # 当前锁定 PyPSA 兼容的 Dataset API 版本
simpeg>=0.25.0,<0.26.0    # SimPEG 电阻率正演

# Web 后端依赖
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# 训练后端：Token Router
torch>=2.5.0,<2.6.0
transformers>=4.46.0,<4.47.0
tokenizers>=0.20.0,<0.21.0
accelerate>=1.1.0,<1.2.0
safetensors>=0.4.0,<0.5.0

# HTTP 客户端，调用 LLM API 与外部服务
requests>=2.28.0

# 开发检查 / 测试
pytest>=7.0.0
ruff>=0.3.0

