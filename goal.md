# PiERN 新数据合成改造目标

## 1. 总目标

在服务器 `/root/data/PierNet` 中新增一套面向普通用户的“新数据合成”前端及配套编排 API。它与现有“数据合成”“简洁训练”“复杂训练”处于同一产品层级，是第四个独立入口，不是旧数据合成的子页面，也不替换旧功能。

新数据合成只负责把用户自己的科学计算数据整理成可供 Router 和 Text2Comp 训练使用的规范数据。用户不需要理解 HDF5、registry、template、Stage 3、Router Parquet 等内部概念，只需完成“接入数据、确认数据定义、生成训练数据”三个步骤。生成完成后，用户可以进入简洁训练或复杂训练继续工作。

必须修复当前 Text2Comp 数据目标不一致、样本填充与训练入口未接通、重复生成重型数据、派生缓存过大和不同 schema 混放等问题。不能只制作静态页面或用假进度掩盖旧链路。

## 2. 产品层级与隔离边界

平台首页和顶层导航应明确展示四个同级模块：

1. 数据合成
2. 新数据合成
3. 简洁训练
4. 复杂训练

新模块的产品名称固定为“新数据合成”，生产访问路径使用 `/new-synth/`。旧 `/synth/*`、`/training/simple/*` 和 `/training/*` 的路由、接口和行为必须保持兼容。

新前端业务代码必须与旧数据合成隔离：

- 建立独立前端目录，优先使用 `frontend-new-synth`，包含自己的 `package.json`、锁文件、Vite/TypeScript 配置、样式、路由、状态、测试和构建目录。
- 不得导入 `frontend/src/synth` 的页面、组件、状态或 CSS，也不得复制旧页面后继续堆叠修改。
- 可以复用稳定的设计令牌、Lucide 图标和后端 OpenAPI 契约，但不能依赖旧数据合成页面的运行状态。
- 主前端只增加“新数据合成”顶层入口和必要的路由交接，不在 `PlatformRouter.tsx` 中承载新模块的业务页面。
- 新模块使用独立的本地存储前缀、错误边界、构建产物和测试。
- 现有 `frontend-studio`、`PierNet/studio` 以及工作区中的其他未提交改动不得被删除或重写；确需复用服务时必须保持原行为兼容。

## 3. 用户流程

### 3.1 接入数据

将以下三种方式合并为一个“数据来源”步骤，它们是可选来源，不是连续任务：

- 运行平台内置仿真生成数据
- 上传用户已有的 HDF5 数据
- 上传用户专家模型并调用它生成数据

用户选择一种方式即可继续。直接上传 HDF5 时，不应强制用户同时上传专家模型。使用专家模型生成数据时，再要求完成专家接口检查。

系统自动校验 `timeseries`、`params`、`param_names`，检查形状、数据类型、有限值和样本数量。`n_samples`、`n_channels`、`n_timesteps`、`n_params` 等可由数组形状推导的根属性由后端自动补齐，不要求用户手工提供。

### 3.2 确认数据定义

把原来的场景注册、参数解释、输出定义、通道选择、时间采样和最终确认合并为一个页面。页面分为三个连续区域：

- 任务与场景：任务名称、领域描述、场景说明
- 输入参数：参数名称、含义、单位、顺序和范围
- 输出结果：输出通道、含义、单位以及通道和时间采样方式

系统应从数据结构和 LLM 自动预填，用户只负责检查和修正。GitHub 地址或外部说明只作为折叠的“辅助识别”选项，不是必填项，也不是下游训练产物。

不同模拟器中的同名场景必须使用 `simulator/scenario` 联合命名，禁止仅以 `scenario` 保存模板或训练产物。

### 3.3 生成训练数据

用户点击一次“生成训练数据”后，后端按顺序自动完成：

1. 固化数据定义版本
2. 生成或复用语言模板缓存
3. 生成 Text2Comp 训练数据
4. 生成当前二分类 Router 训练数据
5. 校验 schema、样本数、类别分布和少量真实样本
6. 将两个训练数据集注册到简洁训练和复杂训练可选择的数据源中

前端只显示当前阶段、真实进度、已完成内容、错误原因和重试操作。模板生成、样本填充和 Router 构建不再作为三个需要用户分别操作的页面。

生成完成页展示两个明确产物：

- Text2Comp 数据集：自然语言输入到专家模型输入参数
- Router 数据集：自然语言输入到目标场景或路由标签

页面提供“进入简洁训练”和“进入复杂训练”两个后续入口，并携带稳定的数据集 ID。训练模块不能依赖目录扫描猜测用户刚生成了哪个数据集。

## 4. 必须统一的数据契约

PiERN 的主链路固定为：

```text
用户问题 -> Router 选择场景或专家 -> Text2Comp 生成专家输入参数 -> Expert 计算物理结果
```

因此必须遵守以下契约：

- Text2Comp 的输出是“专家模型输入参数”。
- Text2Comp 训练样本是 `prompt/input -> expert_input/params`。
- HDF5 的 `timeseries` 或旧 Stage 3 的物理 `target` 是专家输出真值，只用于专家验证、端到端评测或可选结果检查，不能作为 Text2Comp 标签。
- 必须验证 Text2Comp 标签与专家输入的参数数量、顺序、名称、单位、形状、数据类型和归一化方式一致。
- 旧 `data_converter.py` 或任何兼容路径不得继续把物理 `target` 转换为 Text2Comp `label`。
- Router 数据只保留路由训练真正需要的文本、标签和必要元数据，不得为了 Router 强制物化完整时序目标。

第一版继续生成当前二分类 Router 数据，但 schema 和 manifest 中使用明确的类别列表与标签版本，避免把二分类写死在文件解析逻辑中，为以后扩展多分类保留兼容空间。本目标不要求现在实现多分类训练。

## 5. 需要修复的现有问题

1. 统一 Text2Comp 标签定义，删除“物理输出作为 Text2Comp 标签”的错误路径。
2. 将网页 Text2Comp 训练接到新生成的规范数据集，停止从 HDF5 临时再生成一套语义不同的小型 JSONL。
3. 将旧 Stage 3 的重型五字段记录拆成用途明确的 Text2Comp、Router 和可选评测数据，避免生成下游不消费的数值字段。
4. 为每种数据集写入显式 `schema_name`、`schema_version` 和 `artifact_type`，修复 Text2Comp JSONL 被总览识别为未知 Stage 3 数据的问题。
5. 模板、manifest 和产物路径统一使用模拟器与场景联合命名，消除同名覆盖风险。
6. HDF5 可推导属性由服务器生成，上传校验只要求真正不可缺少的数据。
7. 将 `.manifests`、`.indexes`、Parquet-to-JSONL、prepared dataset 等标记为派生缓存，不得作为用户产物或必经步骤展示。
8. 派生缓存使用可配置 TTL，并按最近访问时间续期；清理任务只能删除可重建缓存，不能删除原始 HDF5、已确认数据定义和正式训练数据集。
9. 生成任务必须做磁盘预检、样本数上限和 Router 正负样本膨胀量预估，禁止再次无提示地产生超大缓存。

## 6. 后端编排与状态

新增聚合 API，建议统一放在 `/api/new-synth/*`。前端不得自己串联旧的模板、填充和 Router 接口，也不得依靠轮询多个互不一致的底层状态拼出假进度。

至少提供：

- 创建和恢复数据合成工作流
- 上传或选择数据来源
- 数据检查与自动推断
- 保存确认后的数据定义
- 启动、查询、取消和重试生成任务
- 查询生成事件和真实进度
- 获取最终 Text2Comp/Router 数据集及其质量摘要

每个工作流和任务必须持久化 `waiting/running/succeeded/failed/cancelled`、当前阶段、真实进度、输入版本、产物 ID、错误代码和可执行的下一步。启动操作必须幂等，刷新页面和服务重启后能够恢复。

正式训练数据集应拥有稳定 ID、内容哈希、schema 版本、来源数据版本、定义版本、样本数量、生成配置和创建时间。简洁训练与复杂训练通过数据集 ID 使用产物，不通过绝对路径或模糊目录扫描绑定。

## 7. 页面与视觉要求

新数据合成是工作型工具，不做营销落地页。视觉效果应当像精密科学软件与现代数据工具的结合：安静、清楚、可信、紧凑，但不能沿用旧数据合成密集侧栏和大量深色面板的结构。

- 使用明亮中性色背景、白色内容面、深色正文和克制的青蓝主色；成功、警告、错误使用语义色。
- 禁止渐变背景、发光球、玻璃拟态、大面积紫色或深蓝主题、装饰性大 Hero 和无意义留白。
- 页面最大宽度约 1280px，使用稳定网格和 16 至 24px 间距；卡片圆角不超过 8px，不允许卡片嵌套卡片。
- 顶部只展示模块名称“新数据合成”、工作流名称和三步进度，不展示 Stage 1/2/3/4、registry、Parquet 等内部术语。
- 每一步只保留一个明确主操作；次要设置放入高级选项或抽屉。
- 来源选择使用三个清楚的互斥选项；定义确认使用表单和数据预览；生成阶段使用真实进度与可恢复状态。
- 图标使用 Lucide；按钮文字不得截断；长参数名、长错误和大量样本必须有稳定换行、滚动或虚拟列表。
- 必须支持键盘操作、可见焦点、字段标签、就地错误和状态播报，达到 WCAG 2.2 AA 的基本要求。
- 至少验证 390x844、768x1024、1280x720、1440x900；页面滚动、下拉框、抽屉和长表单不得被固定高度或 `overflow` 锁死。

## 8. 页面范围

新前端只需要以下核心页面或路由状态：

- 工作流列表与继续上次任务
- 接入数据
- 确认数据定义
- 生成训练数据
- 生成结果与跳转训练

不为模板、填充样本、Router 构建、模板浏览、样本浏览、Router 浏览、注册表、文件管理和 LLM 配置分别建立主导航页面。需要调试时，将它们收纳到默认折叠的“技术详情”或独立高级检查入口。

## 9. 实施顺序

1. 审计当前未提交改动和旧数据消费者，建立回归基线。
2. 先统一 Text2Comp、Router 和评测数据契约，并补充契约测试。
3. 修复转换脚本、数据识别、命名空间和缓存生命周期。
4. 实现新数据合成聚合 API、持久化工作流和数据集注册。
5. 创建隔离的新前端并接入平台同级入口。
6. 将生成结果接入简洁训练和复杂训练的数据集选择。
7. 通过前端完整走通至少两种数据来源，修复过程中发现的 BUG。
8. 对所有关键状态和视口截图，逐张审查并调整界面。
9. 运行新旧前后端质量门禁，确认旧模块无回归。

## 10. 验收标准

1. 平台首页存在“新数据合成”同级入口，旧三个模块仍可正常访问。
2. 新前端业务代码、构建、状态和测试与旧数据合成隔离。
3. 普通用户只通过三个步骤即可生成可训练数据，不需要理解内部四阶段。
4. 内置仿真、HDF5 上传和专家生成是三个可选来源，不再被误导为连续必做任务。
5. Text2Comp 标签明确等于专家输入参数；代码、manifest、测试和界面说明保持一致。
6. 物理 `timeseries/target` 不再进入 Text2Comp 标签，Router 构建不再依赖完整物理输出。
7. 新生成的 Text2Comp 和 Router 数据都能在简洁训练与复杂训练中按稳定 ID 选择。
8. 不再出现同名场景覆盖、训练 JSONL 被识别成未知 Stage 3、重复生成两套 Text2Comp 数据等问题。
9. 生成任务支持刷新恢复、取消、失败重试、磁盘预检和真实进度。
10. 派生缓存可以安全清理，正式数据和原始数据不会被 TTL 删除。
11. 使用小规模真实 HDF5 完成一次仅通过前端操作的端到端验证，并检查生成数据的实际字段和消费者。
12. 新前端通过 typecheck、lint、format check、unit、build、Playwright E2E 和视觉截图审查；后端通过 ruff、compile 和 pytest。
13. 旧数据合成、简洁训练、复杂训练的主要页面、API 和测试保持可用。

## 11. 最终交付

完成后必须给出：

- 新增和修改文件清单
- 新旧模块的隔离与路由方式
- 最终 Text2Comp、Router 和可选评测数据 schema
- 被删除、停用或保留兼容的数据路径
- 聚合 API、任务状态和缓存策略
- 新数据如何被简洁训练与复杂训练选择
- 前端完整操作步骤和真实验证结果
- 桌面与移动端关键页面截图
- 所有质量门禁结果、已修 BUG、剩余限制和后续多分类扩展位置

不要提交与本目标无关的重构，不要清理或覆盖工作区中已有的用户改动。未经明确要求不要提交或推送 Git。
