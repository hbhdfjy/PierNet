name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend Quality Gate
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            pyproject.toml

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install -e . --no-deps

      - name: Ruff
        run: python -m ruff check .

      - name: Consistency check
        run: python scripts/ci/check_consistency.py

      - name: Repository hygiene check
        run: python scripts/ci/check_repo_hygiene.py

      - name: Migration readiness check
        run: python scripts/ci/check_migration_ready.py

      - name: OpenAPI export
        run: python scripts/ci/export_openapi.py /tmp/piern-openapi.json

      - name: Set up Node for OpenAPI contract
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.0'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend contract dependencies
        run: npm --prefix frontend ci

      - name: OpenAPI type contract check
        run: npm --prefix frontend run openapi:check

      - name: Pytest
        run: python -m pytest

  frontend:
    name: Frontend Quality Gate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.0'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Test
        run: npm run test

      - name: Build
        run: npm run build

      - name: Install Playwright browser
        run: npx playwright install chromium

      - name: Playwright smoke
        env:
          PIERN_E2E_START_SERVER: '1'
        run: npm run e2e:smoke
